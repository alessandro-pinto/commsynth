/**\page arcnet_delay_model Delay Models
 *
 * \section arcnet_wire_delay_model Wire Delay Model
 *
 * A wire delay model computes the delay of a wire given its length (see cosi::models::WireDelayModel).
 * The input paremeter is the wiring path that is a list of points defining a path from one position to
 * another position.
 *
 * 
 *
 * \section arcnet_bus_delay_model Bus Delay Model
 *
 * 
 *
 *
 * */